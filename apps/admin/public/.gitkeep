# Next.js serves this directory at the site root. Kept in git even when
# empty: the production Dockerfiles COPY it, and an untracked empty dir
# exists on a dev machine but not in a fresh CI checkout.
